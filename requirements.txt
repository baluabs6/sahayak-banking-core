# --- Web framework ---
blacksheep==2.0.7
uvicorn[standard]==0.30.6

# --- Config / validation ---
pydantic==2.9.2
pydantic-settings==2.5.2

# --- PostgreSQL ---
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0
alembic==1.13.2

# --- MongoDB ---
motor==3.6.0

# --- Redis (cache + rate limiting) ---
redis==5.1.1

# --- Auth ---
pyjwt==2.9.0

# --- AWS ---
boto3==1.35.36

# --- ML / data ---
numpy==1.26.4
scikit-learn==1.5.2
pandas==2.2.3

# --- AI / LLM providers ---
anthropic==0.36.2
openai==1.51.2
httpx==0.27.2

# --- LangChain (RAG + provider-agnostic adapter) ---
langchain==0.3.4
langchain-core==0.3.12
langchain-community==0.3.3
langchain-anthropic==0.2.3
langchain-openai==0.2.3
faiss-cpu==1.9.0
sentence-transformers==3.2.0
rank-bm25==0.2.2               # sparse retriever for hybrid search (USE_HYBRID_RETRIEVAL=true)
google-generativeai==0.8.3     # Gemini provider (optional; only needed if LLM_PROVIDER=gemini)

# --- Testing ---
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2
